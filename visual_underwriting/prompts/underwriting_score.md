# Visual Underwriting Scoring Prompt

Assess this {{asset_type}} image for credit underwriting.

Use the metadata for consistency checks but do not invent fields.

Metadata JSON:

{{metadata_json}}

Return JSON matching exactly this schema:

{{schema_json}}
